# Application deployment pipelines

## Component repos

Each application repo owns **Dev/Test** deploy YAML under `DevOps/CI.yml`.

## Central deployment repo

`customer-portal-deployment` owns Acc/Prod manifest flows.

## Exceptions

Document any component that does not use the manifest model (e.g. authorization sync jobs) in a child page here.
