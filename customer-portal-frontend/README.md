# Customer Portal — Frontend

Stub repository for workspace and pipeline layout demos. No application source required for the webinar focus (AI + DevOps).

## CI

`DevOps/CI.yml` — same branching model as backend.

## Shared AI config

Copy from `../ai-tooling-config/` per [ai-tooling-config/README.md](../ai-tooling-config/README.md).
