# Customer Portal — Backend API (.NET)

Minimal API stub for AI-assisted code review and pipeline demos.

## Run locally

```bash
cd src/Portal.Api
dotnet run
```

## CI

See `DevOps/CI.yml` — mirrors a typical develop/main/hotfix trigger model.
